import AboutContent from "@/components/AboutContent";

export const metadata = {
  title: "About | Tiffany Chen Art",
  description: "Biography of Tiffany Chen (陳嬋娟) — gouache painter, oil painter, and sculptor.",
};

export default function AboutPage() {
  return <AboutContent />;
}
